<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <application
        android:name=".TaesungApp"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:label="@string/app_name"
        android:theme="@style/Theme.Taesung">

        <!-- FCM 네이티브 푸시 수신 -->
        <service
            android:name=".FcmService"
            android:exported="false">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT" />
            </intent-filter>
        </service>
        <meta-data
            android:name="com.google.firebase.messaging.default_notification_channel_id"
            android:value="taesung_erp" />

        <!-- 메인: ERP 웹을 전체화면으로 여는 TWA 런처 -->
        <activity
            android:name="com.google.androidbrowserhelper.trusted.LauncherActivity"
            android:label="@string/app_name"
            android:exported="true">

            <meta-data
                android:name="android.support.customtabs.trusted.DEFAULT_URL"
                android:value="@string/app_url" />
            <meta-data
                android:name="android.support.customtabs.trusted.STATUS_BAR_COLOR"
                android:resource="@color/ic_launcher_background" />

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <intent-filter android:autoVerify="true">
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data
                    android:scheme="https"
                    android:host="taesung-urban.duckdns.org" />
            </intent-filter>
        </activity>

        <!-- 위젯 로그인/설정 화면 -->
        <activity
            android:name=".WidgetConfigActivity"
            android:exported="true"
            android:label="@string/app_name" />

        <!-- 푸시 알림 탭 시 뜨는 작은 팝업(다이얼로그) -->
        <activity
            android:name=".NotificationPopupActivity"
            android:theme="@style/Theme.Taesung.Dialog"
            android:taskAffinity="org.duckdns.taesung_urban.twa.widgetpopup"
            android:excludeFromRecents="true"
            android:finishOnTaskLaunch="true"
            android:noHistory="true"
            android:exported="false" />

        <!-- 위젯 날짜 탭 시 뜨는 일정 팝업(다이얼로그) -->
        <activity
            android:name=".DaySchedulePopupActivity"
            android:theme="@style/Theme.Taesung.Dialog"
            android:taskAffinity="org.duckdns.taesung_urban.twa.widgetpopup"
            android:excludeFromRecents="true"
            android:finishOnTaskLaunch="true"
            android:noHistory="true"
            android:exported="false" />

        <!-- 홈 화면 위젯: 오늘 일정 -->
        <receiver
            android:name=".TodayScheduleWidget"
            android:exported="true">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
                <action android:name="com.taesung.widget.REFRESH" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/widget_today_info" />
        </receiver>

    </application>
</manifest>
